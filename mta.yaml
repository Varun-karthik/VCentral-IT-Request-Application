_schema-version: "3.2"
ID: vcentral
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: vcentral-destination-content
  type: com.sap.application.content
  requires:
  - name: vcentral-destination-service
    parameters:
      content-target: true
  - name: vcentral-repo-host
    parameters:
      service-key:
        name: vcentral-repo-host-key
  - name: vcentral-uaa
    parameters:
      service-key:
        name: vcentral-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: vcentral_html_repo_host
          ServiceInstanceName: vcentral-html5-service
          ServiceKeyName: vcentral-repo-host-key
          sap.cloud.service: vcentral
        - Authentication: OAuth2UserTokenExchange
          Name: vcentral_uaa
          ServiceInstanceName: vcentral-xsuaa-service
          ServiceKeyName: vcentral-uaa-key
          sap.cloud.service: vcentral
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: vcentral-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: vcentral-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - vcentral.zip
      name: vcentral
      target-path: resources/
- name: vcentral
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: vcentral-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: vcentral-destination-service
    service-plan: lite
- name: vcentral-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: vcentral-xsuaa-service
    service-plan: application
- name: vcentral-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: vcentral-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
